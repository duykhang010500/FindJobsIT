import React from 'react';
import { Alert, Typography } from '@mui/material';

type Props = {};

const JobSeekerSettings = (props: Props) => {
  return (
    <Alert severity='info'>
      <Typography variant='h5'>This feature is in development</Typography>
    </Alert>
  );
};

export default JobSeekerSettings;
